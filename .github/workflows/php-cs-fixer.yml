name: php-cs-fixer
on: [ push, pull_request ]

jobs:
  php-cs-fixer:
    runs-on: [self-hosted, linux, x64]
    name: Run PHP CS
    steps:
      - uses: actions/checkout@master
      - name: Run PHPCS
        run: |
          phpcs *.php
