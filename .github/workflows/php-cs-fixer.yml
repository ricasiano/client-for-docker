name: php-cs-fixer
on: [ push, pull_request ]

jobs:
  php-cs-fixer:
    runs-on: [self-hosted, linux, x64]
    name: Run PHP CS
    steps:
      - name: Run PHPCS
        run: |
          cat uglypsr.php
          phpcs --standard=PSR12 *.php
